name: Deploy dist to Hostinger via FTP

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Build Project
        run: npm run build 

       - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: ftp.climitra.com
          username: u774597600.testuser
          password: Password@climitra0
          server-dir: /home/u774597600/domains/climitra.com/public_html/
          local-dir: ./dist/
         
